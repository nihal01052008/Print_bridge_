{
  "name": "printbridge",
  "version": "1.0.0",
  "private": true,
  "description": "PrintBridge — cloud printing platform. Run `npm run dev` from this root folder to start both servers at once.",
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,magenta \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "seed:admin": "npm run seed:admin --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
